# Go
bin/
dist/
*.exe
*.test
*.out
vendor/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
build/
*.egg

# Virtual environment
.venv/
venv/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Hugo
.hugo_build.lock
public/
site/.hugo_build.lock
site/public/
site/resources/

# Claude Code
.claude/

# MinIO: extracted static binary staged by `make container-build-minio` right
# before the image build; only embed/minio.Containerfile is committed, never the
# binary itself. (Exact path match — embed/minio.Containerfile stays tracked.)
embed/minio

# mc client: per-arch release binary staged as embed/mc and the CA file, put in
# embed/ by `make container-build-mc` right before each image build and deleted
# after. (Exact paths — embed/mc.Containerfile stays tracked.)
embed/mc
embed/ca-certificates.crt
